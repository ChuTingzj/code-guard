{
  "name": "code-guard",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start:tunnel": "cloudflared tunnel --url http://localhost:3001 --protocol http2",
    "postinstall": "node scripts/link-workspace-env.mjs",
    "env:link": "node scripts/link-workspace-env.mjs",
    "dev": "pnpm env:link && pnpm -r --parallel --filter @code-guard/server --filter @code-guard/web run dev",
    "dev:server": "pnpm env:link && pnpm --filter @code-guard/server dev",
    "dev:web": "pnpm env:link && pnpm --filter @code-guard/web dev",
    "build": "pnpm -r run build",
    "db:migrate": "pnpm env:link && pnpm --filter @code-guard/server prisma:migrate",
    "db:seed": "pnpm env:link && pnpm --filter @code-guard/server prisma:seed",
    "db:generate": "pnpm --filter @code-guard/server prisma:generate",
    "test:unit": "pnpm --filter @code-guard/server test:unit",
    "e2e:checklist": "bash scripts/e2e-checklist.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
